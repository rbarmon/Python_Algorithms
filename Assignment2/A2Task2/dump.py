
# print(sentences[i])
# if temp.connected_nodes[ord(sentences[i][j]) - 97] is None:
#     node = CharacterNode(sentences[i][j])
#     print(node.character)
#     print(node.frequency)
#     # print(node.character)
#
#     temp.connected_nodes[ord(sentences[i][j]) - 97] = node
#     print(self.trie.connected_nodes)
#     print(temp.connected_nodes)
#     if j == len(sentences[i]):
#         temp = self.trie
#         print(self.trie.connected_nodes)
#         print(temp.connected_nodes)
#         continue
#     else:
#         temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#         print(self.trie.connected_nodes)
#         print(temp.connected_nodes)
# else:
#     temp[ord(sentences[i][j]) - 97].updateFrequency()
#     print(node.character)
#     print(node.frequency)
#     if j == len(sentences[i]):
#         temp = self.trie
#         print(self.trie.connected_nodes)
#         print(temp.connected_nodes)
#         continue
#     else:
#         temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#         print(self.trie.connected_nodes)
#         # print(temp.connected_nodes)


# temp = self.trie.connected_nodes
#
# print(trie.connected_nodes)
# print(temp.connected_nodes)
#
# for i in range(len(sentences)):
#     # while len(sentences[i]):
#     for j in range(len(sentences[i])):
#
#         print(sentences[i])
#         if temp[ord(sentences[i][j]) - 97] is None:
#             node = CharacterNode(sentences[i][j])
#             print(node.character)
#             print(node.frequency)
#             # print(node.character)
#
#             temp[ord(sentences[i][j]) - 97] = node
#             print(trie.connected_nodes)
#             print(temp.connected_nodes)
#             if j == len(sentences[i]):
#                 temp = trie.connected_nodes
#                 print(trie.connected_nodes)
#                 print(temp)
#                 continue
#             else:
#                 temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#                 print(trie.connected_nodes)
#                 print(temp)
#         else:
#             temp[ord(sentences[i][j]) - 97].updateFrequency()
#             print(node.character)
#             print(node.frequency)
#             if j == len(sentences[i]):
#                 temp = trie
#                 print(trie.connected_nodes)
#                 print(temp.connected_nodes)
#                 continue
#             else:
#                 temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#                 print(trie.connected_nodes)
#                 print(temp.connected_nodes)
#
#
#         # current =
#
# # trie = temp
# return trie


# for i in range(len(sentences)):
#     # generate
#     for j in range(len(sentences[i])):
#         if trie.connected_nodes[ord(sentences[i][j]) - 97] is None:
#             node = CharacterNode(sentences[i][j])
#             trie.connected_nodes[ord(sentences[i][j]) - 97] = node
#         else:
#             trie[ord(sentences[i][j]) - 97].updateFrequency()
#
#         if j == 0:
#             if trie[ord(sentences[i][j]) - 97] is None:
#                 node = CharacterNode(sentences[i][j])
#                 trie[ord(sentences[i][j]) - 97] = node
#             else:
#                 trie[ord(sentences[i][j]) - 97].updateFrequency()
#         elif j > 0:

# for i in range(len(sentences)):
#     # while len(sentences[i]):
#     for j in range(len(sentences[i])):
#         print(sentences[i])
#         if temp.connected_nodes[ord(sentences[i][j]) - 97] is None:
#             node = CharacterNode(sentences[i][j])
#             print(node.character)
#             print(node.frequency)
#             # print(node.character)
#
#             temp.connected_nodes[ord(sentences[i][j]) - 97] = node
#             print(trie.connected_nodes)
#             print(temp.connected_nodes)
#             if j == len(sentences[i]):
#                 temp = trie
#                 print(trie.connected_nodes)
#                 print(temp.connected_nodes)
#                 continue
#             else:
#                 temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#                 print(trie.connected_nodes)
#                 print(temp.connected_nodes)
#         else:
#             temp[ord(sentences[i][j]) - 97].updateFrequency()
#             print(node.character)
#             print(node.frequency)
#             if j == len(sentences[i]):
#                 temp = trie
#                 print(trie.connected_nodes)
#                 print(temp.connected_nodes)
#                 continue
#             else:
#                 temp = temp.connected_nodes[ord(sentences[i][j]) - 97]
#                 print(trie.connected_nodes)
# print(temp.connected_nodes)



# def addNodes(self, Node, ):

# trie.connected_nodes[ord(sentences[i][j]) - 97].connected_nodes[].


# [None] * 26
# print(ord("a") - 97) #honestly you can make a method i bet
# print(ord("z") - 97)
# sentences = ["abc", "abazacy", "dbcef", "xzz", "gdbc", "abazacy", "xyz",
#              "abazacy", "dbcef", "xyz", "xxx", "xzz"]
